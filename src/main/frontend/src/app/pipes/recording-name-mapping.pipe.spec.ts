import { RecordingNameMappingPipe } from './recording-name-mapping.pipe';

describe('RecordingNameMappingPipe', () => {
  it('create an instance', () => {
    const pipe = new RecordingNameMappingPipe();
    expect(pipe).toBeTruthy();
  });
});
